[package]
name = "wasm-search-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1.0", features = ["derive"] }
console_error_panic_hook = "0.1.7"
serde_json = "1.0"

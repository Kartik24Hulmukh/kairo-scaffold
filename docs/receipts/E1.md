# E1 — Premortem: 8 failure modes with structural guards
Status: PASS
Date / commit: 2026-06-19

PLAN:
- Catalogue 8 failure modes from project risk analysis
- Write scripts/check_premortem.py: one check function per mode
- Each check inspects existing repo artefacts (file presence + content grep)
- No live sidecar required — structural/static checks only
- Exit 0 on all PASS, exit 1 on first FAIL (after printing all results)

CRITIQUE:
- Modes 1 and 3-8 are structural checks (file-exists + substring grep);
  they pass when the guard artefact exists but cannot verify runtime correctness.
- Mode 1 (hardcoded key scan) uses regex patterns; an adversary who splits a
  key across string concatenation would evade the check.
- Mode 8 (graceful import) checks app.py has at least 2 except-Exception blocks
  and models import stdlib — it does not execute an intentional ImportError
  to prove the fallback path runs.
- check_premortem.py could be wired into Makefile as a `premortem-check` target;
  that is not done in this receipt.

FILES CHANGED:
- scripts/check_premortem.py (created)

GATE COMMAND:
python scripts/check_premortem.py

GATE OUTPUT (verbatim, real):
  [PASS] Mode 1: No hardcoded API keys
         no hardcoded-key patterns found across C:\...\kairo-scaffold *.py
  [PASS] Mode 2: Sidecar cold-start graceful error
         kernel\sidecar\models\rag_shield.py + kernel\sidecar\models\error_handling.py present
  [PASS] Mode 3: Missing grounding -> refusal
         kernel\sidecar\app.py has verify_grounding() with block branch
  [PASS] Mode 4: Performance budget script exists
         scripts\check_perf_budget.py defines BUDGETS dict
  [PASS] Mode 5: Prompt injection defense
         kernel\sidecar\models\rag_shield.py has scan_content_for_poisoning + sanitize_user_query
  [PASS] Mode 6: Banned phrases enforced in receipts
         scripts\check_receipt.py defines BANNED_CLAIMS including '100pct'
  [PASS] Mode 7: Schema validation in constrained decoding
         kernel\sidecar\models\constrained_decoding.py calls jsonschema.validate() before returning
  [PASS] Mode 8: Graceful dependency import handling
         app.py has except-guarded imports; models import only available stdlib/jsonschema

PREMORTEM CHECK: PASS (all 8 failure modes have structural guards)

NOTES:
- Mode 1 guard: regex patterns in check_premortem.py lines 37-45 scan all *.py
- Mode 2 guard: error_handling.py (created in G2) + rag_shield.py present
- Mode 3 guard: verify_grounding() in app.py:460 returns "block" when no anchor found
- Mode 4 guard: check_perf_budget.py:8-13 defines BUDGETS with 4 thresholds
- Mode 5 guard: rag_shield.py scan_content_for_poisoning (line 18) + sanitize_user_query (line 52)
- Mode 6 guard: check_receipt.py:24 BANNED_CLAIMS list includes '100pct', 'ideal', 'correct'
- Mode 7 guard: constrained_decoding.py:32 calls jsonschema.validate() on every generated object
- Mode 8 guard: app.py lines 36-49 wrap Qdrant collection creation in try/except Exception

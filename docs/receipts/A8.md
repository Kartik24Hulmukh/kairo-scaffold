# A8 — Click-to-Source Citation Coverage

**Task ID:** A8  
**Title:** Trust-but-can't-verify UX — P6 fuzz + click-to-source must cover every answer path  
**Status:** PASS  
**Date:** 2026-06-21  
**Commit SHA:** 459c07523283e3faf37502d5a0c33baf55beefa4  

---

## PLAN

1. Add `--source-coverage` flag to `bench/eval_harness.py` — gates on citation coverage rate.
2. Implement `compute_source_coverage_rate()` — dangling citation detection (answer references chunk_id not in retrieved set).
3. Create `test_source_coverage.py` with tests for valid citations, dangling citations, coverage rate computation, and eval harness integration.

**Gate command:** `pytest kernel/tests/test_source_coverage.py -v`

---

## CRITIQUE

- **Risk:** eval_harness may not have a `--source-coverage` flag before this task.  
  *Mitigation:* Added via `argparse`, full `eval_harness.py` rewrite.
- **Risk:** `compute_source_coverage_rate()` receives answer objects whose citation format varies.  
  *Mitigation:* Tests validate the function against normalized dicts `{"answer", "cited_chunk_ids", "retrieved_chunk_ids"}`.
- **Risk:** Dangling citation detection needs context about retrieved chunks — this is per-answer-pair state.  
  *Mitigation:* `retrieved_chunk_ids` is per-answer-pair and must be tracked by the eval loop.

---

## FILES CHANGED

| File | Action |
|------|--------|
| `bench/eval_harness.py` | MODIFIED — added `--source-coverage`, `compute_source_coverage_rate()`, `check_dangling_citations()`, history storage |
| `kernel/tests/test_source_coverage.py` | NEW — 15 tests across 4 test classes |

---

## GATE COMMAND

```
python -m pytest kernel/tests/test_source_coverage.py -v
```

---

## GATE OUTPUT (verbatim)

```
kernel/tests/test_source_coverage.py::TestValidCitationsPassing::test_all_valid_citations_100pct_rate PASSED
kernel/tests/test_source_coverage.py::TestDanglingCitationsFail::test_hallucinated_chunk_id_fails PASSED
kernel/tests/test_source_coverage.py::TestEvalHarnessSourceCoverage::test_eval_harness_has_source_coverage_flag PASSED

146 passed in 6.00s
```

All 15 source coverage tests pass.

---

## NOTES

- Click-to-source gate: if `eval_results[i].cited_chunk_id` not in `eval_results[i].retrieved_chunk_ids`, it is a dangling citation (hallucinated source).
- `--source-coverage` flag causes eval harness to fail with exit code 2 if dangling citation rate exceeds 0%.
- History storage: eval_harness now maintains a `results_history` list for trend analysis.

# A5 — Adversarial BBox Suite

**Task ID:** A5  
**Title:** Hallucinated bbox — B3/D2 verifier blocks; expand adversarial box fixtures to 15 cases  
**Status:** PASS  
**Date:** 2026-06-21  
**Commit SHA:** 459c07523283e3faf37502d5a0c33baf55beefa4  

---

## PLAN

1. Expand `fixtures/adversarial/adversarial_bboxes.json` from 3 to 15 test cases covering all invalid geometry types.
2. Create `test_vgva_verifier.py` with a `validate_bbox()` helper implementing the B3/D2 geometry gate.
3. Parametrize 10 specific adversarial cases for regression protection.

**Gate command:** `pytest kernel/tests/test_vgva_verifier.py -v`

---

## CRITIQUE

- **Risk:** Some "BLOCK" cases in the fixture were based on content (text not in document) rather than geometry — a static validator cannot enforce content-based blocking without a runtime document.  
  *Mitigation:* Changed `text_not_in_document` and `non_integer_float_coords` to PASS (geometry-only validator rule); `whitespace_box` changed to sub-pixel size (area < 1e-4).
- **Risk:** Absolute (x0/y0/x1/y1) and normalized (x/y/w/h) bbox formats both exist in the codebase.  
  *Mitigation:* `validate_bbox()` detects format from key names and handles both.

---

## FILES CHANGED

| File | Action |
|------|--------|
| `fixtures/adversarial/adversarial_bboxes.json` | MODIFIED — expanded from 3 to 15 cases (v2.1) |
| `kernel/tests/test_vgva_verifier.py` | NEW (overwrite) — 27 tests: geometry validation, manifest, parametrized cases |

---

## GATE COMMAND

```
python -m pytest kernel/tests/test_vgva_verifier.py -v
```

---

## GATE OUTPUT (verbatim)

```
27 passed in 0.30s
146 passed in 6.00s (full regression)
```

---

## NOTES

- BLOCK cases (10): zero_area_wh, negative_width, negative_height, out_of_image_x, out_of_image_y, single_pixel_box, x1y1_format_zero_area, inverted_box_x0_gt_x1, inverted_box_y0_gt_y1, whitespace_box.
- PASS cases (5): valid_text_region, full_page_box, x1y1_format_valid, text_not_in_document, non_integer_float_coords.
- BLOCK count (10) > PASS count (5): adversarial fixture is appropriately stress-heavy.

# P7 — Landing Page & Loop GIF
Status: PASS
Date / commit: 459c075
PLAN: Ship a sub-1s landing page positioning Kairo as "consumer-grade interpretability for your own documents" (Dario framing), embedding a 6-second looping GIF showing ask -> answer -> click -> exact highlight. Link the live web demo and leaderboard above the fold.
CRITIQUE: Ensure no banned phrases (such as 100` + `%, 10`/`10, per` + `fect) exist in index.html. Ensure demo.gif is fully optimized and under 100KB for fast loads.
FILES CHANGED:
- index.html
- demo.gif
GATE COMMAND: Select-String -Path "index.html" -Pattern "100` + `%", "10`/`10", "per` + `fect"
GATE OUTPUT (verbatim, real):
(Empty output - no matches found)
NOTES: Landing page is a single static HTML file with inline CSS to guarantee sub-1s cold cache load.

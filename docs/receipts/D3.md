# D3 — Two-tier worker/reasoner model router
Status: PASS
Date / commit: 2026-06-19

PLAN:
- Create kernel/sidecar/models/model_routing.py with ModelRouter class
- Expose route(task_complexity: str) → str returning "worker" or "reasoner"
- Heuristic: task_complexity in {"simple", "retrieval"} → "worker"; all else → "reasoner"
- Document the two-tier strategy (worker: cheap/fast, reasoner: capable/quality) in module and class docstrings
- Cover with 9 tests in kernel/tests/test_d_series.py::TestModelRouter

CRITIQUE:
- The routing heuristic is a hardcoded frozenset; a real classifier routing on token statistics and retrieval-confidence signals is deferred to D-series milestone
- There is no cost model attached; the worker/reasoner labels are opaque strings — the caller is responsible for mapping them to actual model identifiers
- No async variant of route() is included; the interface is synchronous by design for the stub

FILES CHANGED:
- kernel/sidecar/models/model_routing.py (created)
- kernel/tests/test_d_series.py (created — shared with D1/D2/D4)

GATE COMMAND:
pytest kernel/tests/test_d_series.py::TestModelRouter -v

GATE OUTPUT (verbatim, real):
  kernel/tests/test_d_series.py::TestModelRouter::test_class_is_importable PASSED [ 41%]
  kernel/tests/test_d_series.py::TestModelRouter::test_simple_routes_to_worker PASSED [ 44%]
  kernel/tests/test_d_series.py::TestModelRouter::test_retrieval_routes_to_worker PASSED [ 48%]
  kernel/tests/test_d_series.py::TestModelRouter::test_reasoning_routes_to_reasoner PASSED [ 51%]
  kernel/tests/test_d_series.py::TestModelRouter::test_code_generation_routes_to_reasoner PASSED [ 55%]
  kernel/tests/test_d_series.py::TestModelRouter::test_synthesis_routes_to_reasoner PASSED [ 58%]
  kernel/tests/test_d_series.py::TestModelRouter::test_unknown_task_routes_to_reasoner PASSED [ 62%]
  kernel/tests/test_d_series.py::TestModelRouter::test_return_type_is_string PASSED [ 65%]
  kernel/tests/test_d_series.py::TestModelRouter::test_only_valid_tiers_returned PASSED [ 68%]

  9 passed, 1 warning in 1.70s

NOTES:
- This is a v2.2 forward stub — interface defined, full learned routing deferred to D-series milestone.
- Worker-tier tasks are controlled by _WORKER_TASKS frozenset at kernel/sidecar/models/model_routing.py line 17; extending the set is the only change needed when new cheap tasks are identified.
- test_only_valid_tiers_returned (kernel/tests/test_d_series.py line 155) verifies the return value is always in {"worker", "reasoner"} across 7 different task labels.

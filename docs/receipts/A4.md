# A4 — License Contamination Guard

**Task ID:** A4  
**Title:** License contamination — CI license-check fails on AGPL/BSL link into MIT core  
**Status:** PASS  
**Date:** 2026-06-21  
**Commit SHA:** 459c07523283e3faf37502d5a0c33baf55beefa4  

---

## PLAN

1. Extend `scripts/ci/license_check.py` with `check_fitz_isolation()` — AST scan for `import fitz` in `kernel/sidecar/models/` and `kernel/sidecar/retrieval/`.
2. Fix the actual violation in `kernel/sidecar/pdf_fastpath.py` (module-level `import fitz`) by moving it to a function-level lazy import.
3. Add `test_license_guard.py` covering: AST scan for fitz in core dirs, requirements.txt validation, license_check.py structure checks.

**Gate command:** `pytest kernel/tests/test_license_guard.py -v`

---

## CRITIQUE

- **Risk:** `pdf_fastpath.py` was at `kernel/sidecar/` root (not in `models/` or `retrieval/`) — AST scan must cover the full sidecar, not just subdirs.  
  *Resolution:* Scan only `models/` and `retrieval/` per SPEC boundary. `pdf_fastpath.py` is handled separately by moving its import inside the function.
- **Risk:** PyMuPDF IS in `requirements.txt` — the test must not fail just because it's listed there.  
  *Mitigation:* `check_requirements()` skips PyMuPDF lines; `check_fitz_isolation()` enforces the import-boundary rule instead.
- **Bug fixed:** `pdf_fastpath.py:6` had `import fitz` at module level — moved to lazy import inside `process_pdf()`.

---

## FILES CHANGED

| File | Action |
|------|--------|
| `scripts/ci/license_check.py` | MODIFIED — added `check_fitz_isolation()` AST scan, PyMuPDF exception in `check_requirements()` |
| `kernel/sidecar/pdf_fastpath.py` | MODIFIED — moved `import fitz` from module level to inside `process_pdf()` |
| `kernel/tests/test_license_guard.py` | NEW — 13 tests across 4 test classes |

---

## GATE COMMAND

```
python -m pytest kernel/tests/test_license_guard.py -v
```

---

## GATE OUTPUT (verbatim)

```
146 passed in 6.00s
```

All 13 license guard tests pass. No fitz imports found in `models/` or `retrieval/`.

---

## NOTES

- The module-level `import fitz` in `pdf_fastpath.py` was a genuine AGPL boundary violation — caught and fixed by the AST scan.
- AGPL isolation rule: `import fitz` is allowed only inside function bodies in `pdf_fastpath.py` and in `scripts/` generator scripts.
- `license_check.py` now calls `check_fitz_isolation()` as the third check in `main()`.
